//0.7.3 B null regression (return form). NLang has no ternary operator —
//if/else form.
int[] mk() { return null; }
int main() {
    int[] a = mk();
    int r = 1;
    if (a == null) { r = 7; }
    return r;
}
