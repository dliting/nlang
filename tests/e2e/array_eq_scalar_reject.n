//0.7.3 B D8: an array against a scalar operand is rejected — the handle
//vs value comparison is silent garbage (== and != included).
int main() {
    int[] a = new int[2];
    if (a == 5) { return 1; }
    return 0;
}
