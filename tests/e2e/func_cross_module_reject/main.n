//Phase 13 Step 2: cross-module negative — the imported stub for dbl
//cannot carry a Func signature; referencing it must be rejected.
import lib;

int main() {
    Func<int, int> f = dbl;
    return f(1);
}
