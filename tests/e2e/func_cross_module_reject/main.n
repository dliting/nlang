//Phase 13 Step 2: cross-module negative — the bare reference to dbl is
//rejected by the module-visibility rule (imported members must be
//qualified), before any Func-signature check runs.
import lib;

int main() {
    Func<int, int> f = dbl;
    return f(1);
}
