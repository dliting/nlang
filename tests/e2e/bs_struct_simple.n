struct Point { int x; int y; }

int main() {
    ByteStream bs = new ByteStream();
    Point p;
    p.x = 10;
    p.y = 20;
    bs.writeStruct(p);
    bs.reset();
    Point q = bs.readStruct("Point");
    return q.x + q.y;
}
