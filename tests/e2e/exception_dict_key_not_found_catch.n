int main() {
    Dict<string, int> d = new Dict<string, int>();
    d.set("a", 1);
    try {
        int v = d.get("missing");
        return v;
    } catch (Exception e) {
        return 1;
    }
    return 0;
}
