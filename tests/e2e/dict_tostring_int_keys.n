// Phase 9b-pre: Dict toString with int keys (not just string keys).
int main() {
    Dict<int, int> d = new Dict<int, int>();
    d.set(10, 100);
    d.set(20, 200);
    string s = d.toString();
    if (s == "{10: 100, 20: 200}") return 7;
    return 1;
}
