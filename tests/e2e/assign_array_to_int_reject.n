//Assigning an array value to an int variable is TCK_Same in degraded
//form (both sides are int), so the cast table cannot see it — the
//guard must key on the array-valued property, not the cast kind.
int main() {
    int[] ia = new int[3];
    int x = ia;
    return 0;
}
