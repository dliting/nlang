//Phase 13 Step 2: cross-module negative — main.n must reject passing a
//function reference as an argument to an imported function. Func
//signatures stay outside the .nmod type-descriptor grammar (it carries
//data types only), so the stub cannot represent the Func formal.
int apply(Func<int, int> f) {
    return f(1);
}
