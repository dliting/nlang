int main() {
    List<string> lst = new List<string>();
    lst.add("ab");
    lst.add("cde");
    int total = 0;
    foreach (string s in lst) {
        int len = s.length();
        total = total + len;
    }
    return total;
}
