// Phase 9c: overload with default parameter — unambiguous when all args provided.
int foo(int a) { return 100; }
int foo(int a, int b = 0) { return 200; }
int main() {
    int r = foo(5, 10);
    if (r == 200) { return 7; }
    return 1;
}
