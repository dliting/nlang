class Point {
    public int x;
    public int y;
}

int main() {
    Point p1 = new Point();
    Point p2 = new Point();
    int selfRef = p1.Equals(p1);
    int diffRef = p1.Equals(p2);
    return selfRef + diffRef;
}
