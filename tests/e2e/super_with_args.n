//Phase 9d-2: super(x, y) forwards arguments to the parent ctor.
class A {
    public int x;
    public int y;
    public int A(int a, int b) { this.x = a; this.y = b; return 0; }
}
class B : A { public int B() { super(40, 2); return 0; } }
int main() {
    B b = new B();
    return b.x + b.y;
}
