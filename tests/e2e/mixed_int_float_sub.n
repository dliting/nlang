int main() {
    //Symmetric with mixed_int_float_add but for subtraction. int - float
    //promotes int to float. 5 - 2.5 = 2.5 (within tolerance).
    float r = 5 - 2.5;
    if (r > 2.4 && r < 2.6) {
        return 9;
    }
    return 0;
}
