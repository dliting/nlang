// stdlib_str_relational_crossmod — relational string compares inside an
// imported module exercise the new _str opcodes through RemapBytecode's
// InstructionStride walk (cross-module merge path).
import "lib";

int main() {
	if (cmp_lt() != 0) return 1;
	if (cmp_gt() != 1) return 2;
	if (cmp_le() != 0) return 3;
	if (cmp_ge() != 1) return 4;
	return 0;
}
