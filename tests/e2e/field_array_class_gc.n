class Box {
    public int[] a;
}

//9d-3 audit: array-typed class field kept live across a real GC
//(class-instance branch of MarkPhase, declared-kind RTK_Array route).
int main() {
    Box b = new Box();
    b.a = new int[2];
    b.a[0] = 7;
    int n = 0;
    while (n < 1500) {
        Box t = new Box();
        n = n + 1;
    }
    return b.a[0];
}
