int main() {
    string[] arr = new string[2];
    arr[0] = "hello";
    arr[1] = "wor";
    int total = 0;
    foreach (string s in arr) {
        int len = s.length();
        total = total + len;
    }
    return total;
}
