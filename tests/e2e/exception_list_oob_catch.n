int main() {
    List<int> lst = new List<int>();
    lst.add(10);
    lst.add(20);
    try {
        return lst.get(5);
    } catch (IndexOutOfBoundsException e) {
        return 1;
    }
    return 0;
}
