//Phase 13 Step 0: alias as a generic type argument (List<Cell>).
using Cell = Dict<string, int>;

int main() {
	List<Cell> l = new List<Cell>();
	Cell c = new Dict<string, int>();
	c["k"] = 7;
	l.add(c);
	return l[0]["k"];
}
