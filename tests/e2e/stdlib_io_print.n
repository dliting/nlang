// stdlib_io_print — io.print writes the string to stdout followed by '\n'.
// The manifest's third column asserts the stdout substring.
import io;
int main() {
    io.print("hello");
    return 0;
}
