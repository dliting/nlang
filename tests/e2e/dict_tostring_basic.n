// Phase 9b-pre: Dict<K,V> toString — basic insertion-ordered rendering.
int main() {
    Dict<string, int> d = new Dict<string, int>();
    d.set("a", 1);
    d.set("b", 2);
    string s = d.toString();
    if (s == "{\"a\": 1, \"b\": 2}") return 7;
    return 1;
}
