int main() {
    List<int> lst = new List<int>();
    lst.Add(1);
    lst.Add(2);
    lst.Add(3);
    lst.Add(4);
    lst.Add(5);
    int sum = 0;
    foreach (int x in lst) {
        if (x == 3) {
            continue;
        }
        sum = sum + x;
    }
    return sum;
}
