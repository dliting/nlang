class Holder { public int v; }

int main() {
    ByteStream bs = new ByteStream();
    bs.writeObject(null);
    bs.reset();
    Holder h = bs.readObject("Holder");
    if (h == null) {
        return 1;
    }
    return 0;
}
