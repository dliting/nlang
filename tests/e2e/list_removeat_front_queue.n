int main() {
    List<int> lst = new List<int>();
    lst.Add(1);
    lst.Add(2);
    lst.Add(3);
    int sum = lst.Get(0);
    lst.RemoveAt(0);
    sum = sum + lst.Get(0);
    lst.RemoveAt(0);
    sum = sum + lst.Get(0);
    return sum;
}
