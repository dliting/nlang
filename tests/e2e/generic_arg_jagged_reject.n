//List<int[][]> declaration — a jagged generic type argument has no VM
//layout and must be rejected at the declaration, not only at new-expr
//sites.
int main() {
	List<int[][]> xs = new List<int[][]>();
	return 0;
}
