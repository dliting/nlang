//Returning an array value from an int-typed function must be
//rejected: the return coercion is TCK_Same in degraded form, so the
//raw handle would pass through as an int.
int[] mk() {
    return new int[3];
}
int steal() {
    int[] ia = mk();
    return ia;
}
int main() {
    return steal();
}
