interface IWorker {
    public int run();
}

class FastWorker implements IWorker {
    public int run() {
        return 50;
    }
}

class Container {
    public IWorker worker;
}

int main() {
    Container c = new Container();
    c.worker = new FastWorker();
    return c.worker.run();
}
