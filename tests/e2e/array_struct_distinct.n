struct Point {
    int x;
    int y;
}

//Phase 9d-3: each element is a distinct materialized struct — writing
//arr[0] must not alias arr[1].
int main() {
    Point[] arr = new Point[2];
    arr[0].x = 5;
    if (arr[1].x == 0) { return 1; }
    return 0;
}
