int main() {
    //compile_error: float label on an int discriminant — write `case 1:`
    //instead. No implicit promotion for case labels.
    int x = 1;
    switch (x) {
        case 1.5: return 1;
        default: return 2;
    }
}
