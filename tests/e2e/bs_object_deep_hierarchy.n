class A { public int x; }
class B : A { public int y; }
class C : B { public int z; }

int main() {
    ByteStream bs = new ByteStream();
    C c = new C();
    c.x = 1;
    c.y = 2;
    c.z = 3;
    bs.writeObject(c);
    bs.reset();
    A a = bs.readObject("A");
    return a.x;
}
