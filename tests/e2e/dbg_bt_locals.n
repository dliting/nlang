//e2e dbg_bt_locals: backtrace across frames + locals with values.
class Point {
    int x;
    int y;
}
int scale(Point p, int k) {
    int sx = p.x * k;
    return sx + p.y * k;
}
int main() {
    Point p = new Point{x: 2, y: 3};
    int r = scale(p, 3);
    return 0;
}
