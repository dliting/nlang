// cross_mod_array_member — property query paths through the import
// stub (SynthTypeExpr rebuilt return): subscript and .length directly
// on the cross-module call result, no local intermediate.
int[] mk(int n) {
    int[] a = new int[n];
    a[0] = 7;
    return a;
}
