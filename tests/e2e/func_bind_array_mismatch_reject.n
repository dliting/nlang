//Func<int> g = mk where mk returns int[] must be a named rejection
//(silent mis-bind today; the return type's array-ness degrades to the
//element field, so the pointer-identity match cannot see it).
int[] mk() { return new int[2]; }
int main() {
    Func<int> g = mk;
    return 0;
}
