int pick(int x, out int r) {
    if (x > 0) {
        r = 10;
        return 1;
    }
    r = 20;
    return 2;
}

int main() {
    int a = 0;
    int b = 0;
    int p = pick(5, out a);
    int q = pick(-5, out b);
    return a + b + p + q;  // 10 + 20 + 1 + 2 = 33
}
