// Out parameters cannot have default values — the grammar has no
// `out T name = expr` production (an out param is callee-assigned).
int foo(out int x = 5) {
    x = 1;
    return 0;
}

int main() {
    return 0;
}
