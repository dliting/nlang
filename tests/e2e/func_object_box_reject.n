//Phase 13 Step 1: boxing a function handle into Object is rejected —
//boxed slots are invisible to GC.
int bar(int x) { return x; }

int main() {
    Func<int, int> f = bar;
    Object o = f;
    return 0;
}
