int main() {
	//P3.10 OP_Box fix history: boxed int 0 once crashed toString. Now
	//boxed receivers format their payload, so the result is exactly "0".
	Object o = 0;
	string s = o.toString();
	if (s == "0") return 7;
	return 1;
}
