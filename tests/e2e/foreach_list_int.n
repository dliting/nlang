int main() {
    List<int> lst = new List<int>();
    lst.add(1);
    lst.add(2);
    lst.add(3);
    lst.add(4);
    int sum = 0;
    foreach (int x in lst) {
        sum = sum + x;
    }
    return sum;
}
