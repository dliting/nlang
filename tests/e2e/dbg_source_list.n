//e2e dbg_source_list: `l` shows a source window with a stop marker.
int fib(int n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}
int main() {
    int r = fib(10);
    return r - 55;
}
