//compile_error: a List<T[]> class field must carry the declaration flag
//(ResolveDataTypes records tree declarations) — reading its element and
//calling a method on it is the same masqueraded array value as through
//a local, and the receiver gate must reject it.
class Box {
    List<int[]> rows;
    void probe() {
        io.print(rows.get(0).length());
    }
}
int main() { return 0; }
