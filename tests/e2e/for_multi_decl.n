//The for-init multi-declarator path (InitExtras) must keep declarator
//order: j sees i's already-assigned value (j = 20, loop runs 18 times).
int main() {
    int s = 0;
    for (int i = 2, j = i * 10; j > i; j = j - 1) {
        s = s + 1;
    }
    return s;
}
