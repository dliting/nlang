//Assigning an array value to an Object variable must be rejected:
//the array flows as its degraded element type (int), so a TCK_Box
//wrap would box the raw handle bits as an int.
int main() {
    int[] ia = new int[3];
    Object o = ia;
    return 0;
}
